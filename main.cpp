// A simple program that computes the square root of a number
#include <stdio.h>
#include <stdlib.h>

#include "ApplicationConfig.h"

int main (int argc, char *argv[])
{
    printf("Test: %d.%d", Application_VERSION_MAJOR, Application_VERSION_MINOR);

    return 0;
}
